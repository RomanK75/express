class User {
  constructor(id, name, birthdate, phone, document, work, authKey) {
    this.id = id;
    this.name = name;
    this.birthdate = birthdate;
    this.phone = phone;
    this.document = document;
    this.work = work;
    this.authKey = authKey;
  }
}

export default User;
